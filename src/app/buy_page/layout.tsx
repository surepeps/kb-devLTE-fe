/** @format */

import React from 'react';

export default function BuyPageLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <div className="buy-page-layout">
      {children}
    </div>
  );
}
